name: Update Trend Radar

on:
  schedule:
    # Runs daily at 01:00 UTC (9:00 AM MYT)
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  collect-radar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Syntax check collector
        run: |
          python3 -m py_compile tools/collect_radar.py
          echo "syntax OK"

      - name: Run collector (Gemini clustering)
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          python3 tools/collect_radar.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add public/radar.json tools/radar_history.json
          git diff --quiet && git diff --staged --quiet || (
            git commit -m "chore: update radar.json [skip ci]"
            # Another collector may commit between our commit and push; rebase
            # and retry so concurrent runs never fail on a stale ref.
            for i in 1 2 3; do
              if git push; then break; fi
              git pull --rebase --autostash && git push && break
            done)
